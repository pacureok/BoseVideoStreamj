/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir configuraciones específicas de Next.js
  // Por ejemplo, para habilitar características experimentales o configurar imágenes.
  // output: 'standalone', // Si necesitas un output de Docker o similar
  // swcMinify: true, // Habilita la minificación con SWC para mayor velocidad
};

export default nextConfig;
